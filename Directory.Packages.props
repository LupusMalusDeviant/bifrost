<Project>

  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <CentralPackageTransitivePinningEnabled>true</CentralPackageTransitivePinningEnabled>
  </PropertyGroup>

  <ItemGroup>
    <!-- MCP-Protokoll (ADR-0002). SDK 2.0.0 spricht die Spec-Revision 2026-07-28 und handelt mit
         aelteren Gegenstellen 2025-11-25 und frueher aus (Down-Level-Interop im SDK).
         Die Aufteilung ist neu: .Core ist Client + Low-Level-Server (reicht fuer Bifrost.Upstream),
         das Hauptpaket bringt Hosting/DI und Attribut-Discovery dazu, .AspNetCore den Streamable-
         HTTP-Server. Extensions.Tasks bleibt bewusst DRAUSSEN: Unsere Vorgaenge laufen ueber den
         eigenen ITaskStore und die REST-Fassade (ADR-0019, Polling ist der Vertrag) — und die
         SDK-Tasks aus 1.4.x sind zu den neuen ohnehin nicht drahtkompatibel. -->
    <PackageVersion Include="ModelContextProtocol" Version="2.0.0" />
    <PackageVersion Include="ModelContextProtocol.Core" Version="2.0.0" />
    <PackageVersion Include="ModelContextProtocol.AspNetCore" Version="2.0.0" />
    <!-- Serverseitige Argument-Validierung im Lazy-Pfad (ADR-0003, Plan-Risiko R3; MIT) -->
    <PackageVersion Include="JsonSchema.Net" Version="9.4.0" />

    <!-- Hosting / ASP.NET Core -->
    <PackageVersion Include="Microsoft.Extensions.Hosting" Version="10.0.10" />
    <PackageVersion Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.10" />
    <PackageVersion Include="Microsoft.AspNetCore.Components.Web" Version="10.0.10" />

    <!-- Metriken-Export (FR-26). OTLP statt Prometheus-Exporter: letzterer ist nicht stabil
         veröffentlicht; Prometheus-Nutzer scrapen über den OTel-Collector. -->
    <PackageVersion Include="OpenTelemetry.Extensions.Hosting" Version="1.17.0" />
    <PackageVersion Include="OpenTelemetry.Exporter.OpenTelemetryProtocol" Version="1.17.0" />
    <PackageVersion Include="OpenTelemetry.Instrumentation.AspNetCore" Version="1.17.0" />

    <!-- Ed25519-Verifikation der Connector-Manifeste (ADR-0016). .NET 10 bringt Ed25519 nicht
         öffentlich mit; BouncyCastle ist MIT, rein verwaltet (kein natives Beipack, also auch auf
         arm64 unverändert) und geprüfter Code. Eine eigene Kurvenarithmetik an einer
         Vertrauensgrenze wäre die schlechtere Wahl, egal wie kurz sie ausfiele. -->
    <PackageVersion Include="BouncyCastle.Cryptography" Version="2.7.0" />

    <!-- Token-Prüfung, wenn der Gateway selbst Resource Server ist (MCP-Autorisierung).
         Bringt JWKS-Abruf mit Zwischenspeicher und Schlüsselrotation mit — genau das, was man
         nicht selbst schreiben will, wenn eine Signaturprüfung davon abhängt. -->
    <PackageVersion Include="Microsoft.IdentityModel.Protocols.OpenIdConnect" Version="8.22.0" />

    <!-- Persistenz (ADR-0007) + Secret-Verschlüsselung (NFR-04) -->
    <PackageVersion Include="Microsoft.EntityFrameworkCore.Sqlite" Version="10.0.10" />
    <PackageVersion Include="Microsoft.EntityFrameworkCore.Design" Version="10.0.10" />
    <PackageVersion Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="10.0.3" />
    <PackageVersion Include="Microsoft.AspNetCore.DataProtection" Version="10.0.10" />
    <!-- Transitive Pins: EF Sqlite 10.0.10 zieht 2.1.11 mit bekannter CVE (GHSA-2m69-gcr7-jv3q) -->
    <PackageVersion Include="SQLitePCLRaw.bundle_e_sqlite3" Version="2.1.12" />
    <PackageVersion Include="SQLitePCLRaw.lib.e_sqlite3" Version="2.1.12" />
    <PackageVersion Include="SQLitePCLRaw.core" Version="2.1.12" />

    <!-- Tests. AwesomeAssertions statt FluentAssertions (NFR-10): FluentAssertions steht ab v8
         unter kommerzieller Xceed-Lizenz; auf 7.x zu bleiben wäre zwar lizenzrechtlich sauber,
         aber eine tote Linie ohne Weiterentwicklung. AwesomeAssertions ist der Apache-2.0-Fork
         des Codes von vor dem Lizenzwechsel, API-gleich — nur Paketname und Namespace ändern sich. -->
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="18.8.1" />
    <PackageVersion Include="Microsoft.AspNetCore.Mvc.Testing" Version="10.0.10" />
    <PackageVersion Include="xunit.v3" Version="3.2.2" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="3.1.5" />
    <PackageVersion Include="coverlet.collector" Version="10.0.1" />
    <PackageVersion Include="AwesomeAssertions" Version="9.5.0" />
    <PackageVersion Include="Microsoft.Extensions.TimeProvider.Testing" Version="10.8.0" />
    <PackageVersion Include="Testcontainers.PostgreSql" Version="4.13.0" />
  </ItemGroup>

</Project>
